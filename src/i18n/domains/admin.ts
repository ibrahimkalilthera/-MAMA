/**
 * Domain: admin — admin/settings copy: roles, class promotion, Excel import,
 * confirmations, toasts, offline sync.
 *
 * Barrel over the en/fr dictionaries (split out of this file to stay under the
 * line budget); src/i18n/translations.ts imports them from here in historical
 * order so both dictionaries keep the original key order.
 */
export { en } from './adminEn';
export { fr } from './adminFr';
